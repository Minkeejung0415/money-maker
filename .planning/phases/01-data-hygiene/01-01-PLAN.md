---
phase: 01-data-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate_picks.py
  - scripts/cache_hygiene.py
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Baseline validation run always starts with a deterministic cache state."
    - "Validation logs show exactly how many .pkl files were removed before execution."
  artifacts:
    - path: "scripts/cache_hygiene.py"
      provides: "Reusable cache purge utility for data/.prop_cache/*.pkl"
      min_lines: 20
      contains: "def purge_prop_cache"
    - path: "scripts/validate_picks.py"
      provides: "Pre-run hygiene gate that calls the cache purge utility"
      contains: "purge_prop_cache"
  key_links:
    - from: "scripts/validate_picks.py"
      to: "scripts/cache_hygiene.py"
      via: "import and pre-run function call"
      pattern: "from scripts\.cache_hygiene import purge_prop_cache"
---

<objective>
Implement deterministic cache hygiene so every Phase 1 validation run starts clean.

Purpose: Eliminate stale .pkl reuse risk before baseline measurement.
Output: Reusable purge utility and integrated pre-run hygiene logging in validation entrypoint.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@scripts/validate_picks.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reusable prop-cache purge utility</name>
  <files>scripts/cache_hygiene.py</files>
  <action>Create a focused utility with a `purge_prop_cache(cache_dir: Path) -> int` function that deletes only `*.pkl` files in `data/.prop_cache`, returns deleted count, and raises on unexpected filesystem errors. Keep behavior deterministic and side-effect scoped to the target directory.</action>
  <verify>python -c "from pathlib import Path; from scripts.cache_hygiene import purge_prop_cache; p=Path('data/.prop_cache'); p.mkdir(parents=True, exist_ok=True); print('deleted=', purge_prop_cache(p))"</verify>
  <done>Utility exists, is importable, and reports deleted count without touching non-pkl files.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce hygiene gate at validation startup</name>
  <files>scripts/validate_picks.py</files>
  <action>Wire the purge utility into `main()` so cache cleanup runs before mode execution. Log cache directory and deleted count in clear, grep-friendly lines. Ensure the script fails fast if purge fails instead of continuing with uncertain state.</action>
  <verify>python scripts/validate_picks.py --date 2026-03-11</verify>
  <done>Run output includes hygiene gate lines and deleted file count before any validation calculations.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python scripts/validate_picks.py --date 2026-03-11` prints cache purge count at startup
- [ ] `scripts/cache_hygiene.py` only removes `*.pkl` under `data/.prop_cache`
- [ ] Validation still executes after successful purge
</verification>

<success_criteria>
- All tasks completed
- DATA-01 implemented with deterministic, logged cache purge
- No validation path can run before hygiene gate executes
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-hygiene/01-01-SUMMARY.md`
</output>
