---
phase: 38
plan: "01"
type: implementation
wave: 1
depends_on: []
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
---

# Phase 38 Plan 01: MLB Data Source Resilience

## Objective

Make MLB scanner runtime independent from blocked Fangraphs/pybaseball stat calls and expose truthful source/fallback context.

## Tasks

1. Add an opt-in external-stat flag to the scanner.
2. Make `build_live_player_features(...)` skip pybaseball/Fangraphs-backed lookups by default.
3. Label artifact-team-state fallback distinctly from external team pitching.
4. Add tests for default no-external behavior and scanner flag propagation.
5. Add documentation for runtime-required versus optional MLB data sources.

## Verification

- `pytest` targeted MLB scanner/live-feature tests.
- Smoke run `scripts/mlb_scanner.py --date 2026-06-28 --validate --individual-only`.
