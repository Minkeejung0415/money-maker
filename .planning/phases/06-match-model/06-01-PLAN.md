---
phase: 06-match-model
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - alpha/engines/sports/wc_model.py
  - tests/unit/engines/test_wc_model.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04

must_haves:
  truths:
    - "A group-stage WC game dict passes through predict() and receives win_prob + draw_prob + loss_prob summing to 1.0"
    - "A knockout-stage WC game dict receives draw_prob=0.0 and knockout=True from predict()"
    - "Passing a non-WC game dict (league != 'wc') to predict() raises ValueError"
    - "win_prob diverges from sportsbook implied odds by >5pp → elo_edge=True in output"
    - "WCMatchModel instantiates and runs correctly when wc_stats.pkl is absent (Elo-only fallback)"
    - "Missing wc_priors.json causes WCMatchModel() to raise FileNotFoundError immediately"
    - "wc_model.py contains no import from soccer_model.py (confirmed by inspect.getsource)"
  artifacts:
    - path: "alpha/engines/sports/wc_model.py"
      provides: "WCMatchModel class with predict() and evaluate_bet()"
      exports:
        - "WCMatchModel"
        - "_WC_DRAW_RATE"
        - "KNOCKOUT_STAGES"
    - path: "tests/unit/engines/test_wc_model.py"
      provides: "Full unit test coverage of WCMatchModel"
      min_lines: 100
  key_links:
    - from: "alpha/engines/sports/wc_model.py"
      to: "alpha/data/ingestion/wc_elo.py"
      via: "load_wc_elo_ratings() called in __init__"
      pattern: "load_wc_elo_ratings"
    - from: "alpha/engines/sports/wc_model.py"
      to: "alpha/data/ingestion/wc_stats.py"
      via: "get_wc_team_stats() called in __init__ with try/except"
      pattern: "get_wc_team_stats"
    - from: "alpha/engines/sports/wc_model.py"
      to: "alpha/engines/sports/ev_calculator.py"
      via: "EVCalculator(min_edge=min_edge) stored as self.ev_calc"
      pattern: "EVCalculator"
---

<objective>
Implement WCMatchModel — a standalone Elo-logistic W/D/L prediction model for WC 2026 matches.
The model reads from the Phase 5 data layer (wc_elo.py + wc_stats.py) and produces calibrated
group-stage W/D/L probabilities (or Win-to-Advance in knockouts) with a market divergence flag.
WC games NEVER route through SoccerModel.

Purpose: All four MODEL requirements (MODEL-01 through MODEL-04) are satisfied by this single
file. Phase 7 (wc_sgp_builder.py + wc_scanner.py) consumes predict() output fields directly.

Output:
- alpha/engines/sports/wc_model.py — WCMatchModel class
- tests/unit/engines/test_wc_model.py — full unit test suite (TDD: tests first)
</objective>

<execution_context>
@C:/Users/justi/Documents/money-maker/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/Documents/money-maker/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/justi/Documents/money-maker/.planning/PROJECT.md
@C:/Users/justi/Documents/money-maker/.planning/ROADMAP.md
@C:/Users/justi/Documents/money-maker/.planning/phases/06-match-model/06-CONTEXT.md
@C:/Users/justi/Documents/money-maker/.planning/phases/06-match-model/06-PATTERNS.md
@C:/Users/justi/Documents/money-maker/.planning/phases/05-data-foundation/05-01-SUMMARY.md
@C:/Users/justi/Documents/money-maker/.planning/phases/05-data-foundation/05-02-SUMMARY.md

<interfaces>
<!-- Phase 5 contracts delivered — use these directly, no exploration needed. -->

From alpha/data/ingestion/wc_elo.py (05-02-SUMMARY):
  load_wc_elo_ratings() -> dict[str, int]
    Reads data/wc_priors.json. Raises FileNotFoundError if missing.
    Example: {"Brazil": 2089, "Germany": 1980, ...}

  get_elo_rating(team: str, ratings: dict[str, int]) -> int
    Returns rating for team, or 1500 fallback with logger.warning if not found.

From alpha/data/ingestion/wc_stats.py (05-02-SUMMARY):
  get_wc_team_stats() -> dict[str, dict]
    Reads data/.wc_cache/wc_stats.pkl. Raises FileNotFoundError if missing.
    Output per team: {"avg_goals": float, "avg_xG": float, "avg_shots": float, "defense_score": float}

From alpha/data/ingestion/football_data_client.py (05-01-SUMMARY):
  Game dict shape from fetch_wc_games():
    {
      "home_team": str, "away_team": str,
      "home_odds": int, "away_odds": int,
      "league": "wc",
      "event_id": str,
      "commence_time": str,
      "stage": str,   # "GROUP_STAGE" | "LAST_16" | "QUARTER_FINALS" | "SEMI_FINALS" | "THIRD_PLACE" | "FINAL"
      "group": str,   # "Group A" ... "Group L" or "" in knockout rounds
    }

From alpha/engines/sports/ev_calculator.py:
  class EVCalculator:
    def __init__(self, min_edge: float = 0.03): ...
    def american_to_decimal(self, american_odds: int | float) -> float: ...
    def implied_prob(self, decimal_odds: float) -> float:
      return 1.0 / decimal_odds
    def has_edge(self, model_prob: float, decimal_odds: float) -> bool:
      ev = self.expected_value(model_prob, decimal_odds)
      return ev >= self.min_edge
    def expected_value(self, model_prob: float, decimal_odds: float) -> float:
      return model_prob * (decimal_odds - 1) - (1 - model_prob)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for WCMatchModel</name>
  <files>tests/unit/engines/test_wc_model.py</files>

  <read_first>
    - alpha/engines/sports/ev_calculator.py — EVCalculator interface (all methods)
    - tests/unit/engines/test_mlb_model.py — monkeypatch-at-init pattern + evaluate_bet no-edge assertion
    - tests/unit/test_wc_priors_loader.py — generator-throw FileNotFoundError monkeypatch pattern
    - tests/unit/test_wc_stats.py — get_wc_team_stats monkeypatch pattern
    - .planning/phases/06-match-model/06-PATTERNS.md — all test patterns (lines 201-391)
  </read_first>

  <behavior>
    Module-level constants:
    - _FAKE_ELO: dict with at least Brazil, Germany, France, Argentina entries (int values)
    - _FAKE_STATS: dict with Brazil and Germany entries, shape {"avg_goals": float, "avg_xG": float, "avg_shots": float, "defense_score": float}

    @pytest.fixture model(monkeypatch):
    - monkeypatch.setattr("alpha.engines.sports.wc_model.load_wc_elo_ratings", lambda: _FAKE_ELO)
    - monkeypatch.setattr("alpha.engines.sports.wc_model.get_wc_team_stats", lambda: _FAKE_STATS)
    - returns WCMatchModel()

    Test: test_predict_returns_required_keys(model)
    - game dict: home_team="Brazil", away_team="Germany", home_odds=-130, away_odds=110, league="wc"
    - assert all of: win_prob, draw_prob, loss_prob, elo_edge, knockout, model_name, elo_diff in result

    Test: test_predict_probs_sum_to_one_group_stage(model)
    - game dict: stage="GROUP_STAGE", league="wc", home_team="Brazil", away_team="Germany"
    - assert win_prob + draw_prob + loss_prob == pytest.approx(1.0, abs=0.001)

    Test: test_predict_raises_for_non_wc_league(model)
    - game dict: home_team="Chelsea", away_team="Arsenal", league="epl"
    - assert raises ValueError matching "league='wc'"

    Test: test_knockout_suppresses_draw(model)
    - game dict: stage="QUARTER_FINALS", league="wc", home_team="Brazil", away_team="Germany"
    - assert draw_prob == 0.0 and knockout is True

    Test: test_group_stage_has_nonzero_draw(model)
    - game dict: stage="GROUP_STAGE", league="wc", home_team="Brazil", away_team="Germany"
    - assert draw_prob > 0.0 and knockout is False

    Test: test_knockout_probs_sum_to_one(model)
    - game dict: stage="LAST_16", league="wc", home_team="Brazil", away_team="Germany"
    - assert win_prob + draw_prob + loss_prob == pytest.approx(1.0, abs=0.001)

    Test: test_init_raises_when_elo_missing(monkeypatch)
    - monkeypatch load_wc_elo_ratings to raise FileNotFoundError via generator-throw pattern
    - assert raises FileNotFoundError

    Test: test_init_succeeds_when_stats_missing(monkeypatch)
    - monkeypatch load_wc_elo_ratings to return _FAKE_ELO
    - monkeypatch get_wc_team_stats to raise FileNotFoundError via generator-throw pattern
    - model = WCMatchModel() — must not raise
    - assert model._wc_stats == {}

    Test: test_evaluate_bet_returns_none_when_no_edge(monkeypatch)
    - model = WCMatchModel(min_edge=0.99) (impossibly high threshold)
    - game: Brazil vs Germany, league="wc", home_odds=-130, away_odds=110
    - assert model.evaluate_bet(game) is None

    Test: test_evaluate_bet_returns_game_when_edge_exists(monkeypatch)
    - model = WCMatchModel(min_edge=0.0) (always has edge)
    - game: Brazil vs Germany, league="wc", home_odds=+200 (underdog price on strong Elo team)
    - assert model.evaluate_bet(game) is not None
    - assert "win_prob" in result (game dict returned with model fields merged)

    Test: test_elo_edge_flag_set_when_divergence_exceeds_5pp(model)
    - Create game where Brazil Elo dominates Germany sufficiently for win_prob vs -110 default to diverge >5pp
    - Use Brazil Elo=2100, Germany Elo=1800 in _FAKE_ELO for this test (large diff ensures win_prob >> 0.574)
    - Use fixture variant with high-diff Elo or pass explicit home_odds that creates divergence
    - assert result["elo_edge"] is True

    Test: test_model_name_is_wc_elo_logistic(model)
    - game: Brazil vs Germany, league="wc"
    - assert result["model_name"] == "wc_elo_logistic"

    Test: test_wc_model_does_not_import_soccer_model()
    - import inspect and alpha.engines.sports.wc_model as wc_mod
    - source = inspect.getsource(wc_mod)
    - assert "soccer_model" not in source

    Test: test_xg_modifier_applied_when_stats_available(model)
    - game: Brazil vs Germany, league="wc" (both have _FAKE_STATS entries)
    - result = model.predict(game)
    - assert result["elo_diff"] != (elo["Brazil"] - elo["Germany"])
      (elo_diff should reflect xG adjustment, not raw Elo diff)

    Test: test_xg_modifier_skipped_when_team_not_in_stats(model)
    - Use teams not in _FAKE_STATS (e.g. France vs Argentina with empty stats)
    - game: France vs Argentina, league="wc" (neither in _FAKE_STATS)
    - result = model.predict(game)
    - assert result["elo_diff"] == pytest.approx(elo["France"] - elo["Argentina"], abs=0.01)
      (raw Elo diff used when stats absent)

    Test: test_evaluate_batch_returns_list(model)
    - model = WCMatchModel(min_edge=0.0) via fixture
    - games = [game_brazil_germany, game_france_argentina] (both valid wc league dicts)
    - result = model.evaluate_batch(games)
    - assert isinstance(result, list) and len(result) == 2
  </behavior>

  <action>
    Create tests/unit/engines/test_wc_model.py following the patterns in 06-PATTERNS.md.

    File header: module docstring "Tests for alpha/engines/sports/wc_model.py."

    Imports (exact): from __future__ import annotations, import inspect, import pytest,
    from alpha.engines.sports.wc_model import WCMatchModel, _WC_DRAW_RATE, KNOCKOUT_STAGES

    Constants: _FAKE_ELO maps Brazil=2100, Germany=1980, France=2050, Argentina=2070.
    _FAKE_STATS maps Brazil and Germany only (France and Argentina intentionally absent —
    enables test_xg_modifier_skipped_when_team_not_in_stats).

    Fixture: @pytest.fixture named "model" with monkeypatch parameter. Apply both
    setattr patches before calling WCMatchModel(). Return the model instance.

    FileNotFoundError monkeypatch: use generator-throw pattern from 06-PATTERNS.md line 296:
      lambda: (_ for _ in ()).throw(FileNotFoundError("..."))

    No fenced code blocks in this action. Tests must fail (ImportError on WCMatchModel) when run
    before Task 2. Commit message after RED: "test(06-01): add failing WCMatchModel tests"

    Run to confirm RED: ./venv/Scripts/python.exe -m pytest tests/unit/engines/test_wc_model.py -x 2>&1 | head -20
    Expected: ImportError or ModuleNotFoundError — test file collected, module import fails.
  </action>

  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/engines/test_wc_model.py -x 2>&1 | grep -E "(FAILED|ERROR|ImportError|ModuleNotFoundError|error)" | head -5</automated>
  </verify>

  <acceptance_criteria>
    - tests/unit/engines/test_wc_model.py exists and is collected by pytest
    - Running the test file returns at least 1 ERROR or FAILED (module does not exist yet)
    - The file imports WCMatchModel, _WC_DRAW_RATE, KNOCKOUT_STAGES from alpha.engines.sports.wc_model
    - At minimum 15 test functions present (grep "^def test_" count >= 15)
    - No syntax errors in the test file (python -c "import ast; ast.parse(open('tests/unit/engines/test_wc_model.py').read())" exits 0)
  </acceptance_criteria>

  <done>
    Test file committed with all 15+ test cases. Running the suite produces failures/errors
    because wc_model.py does not exist yet. RED phase confirmed.
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement WCMatchModel to pass all tests</name>
  <files>alpha/engines/sports/wc_model.py</files>

  <read_first>
    - alpha/engines/sports/soccer_model.py — class structure, EVCalculator wiring, evaluate_batch pattern
    - alpha/engines/sports/ev_calculator.py — american_to_decimal, implied_prob, has_edge signatures
    - alpha/data/ingestion/wc_elo.py — load_wc_elo_ratings, get_elo_rating signatures
    - alpha/data/ingestion/wc_stats.py — get_wc_team_stats signature
    - .planning/phases/06-match-model/06-CONTEXT.md — all formulas and output field spec
    - .planning/phases/06-match-model/06-PATTERNS.md — exact constructor and predict() patterns
  </read_first>

  <action>
    Create alpha/engines/sports/wc_model.py. Do not import from soccer_model.py at any point.

    Module docstring: "Elo-logistic match model for WC 2026. Standalone — never routes through SoccerModel."

    Imports block (exact order):
      from __future__ import annotations
      import logging
      from alpha.engines.sports.ev_calculator import EVCalculator
      from alpha.data.ingestion.wc_elo import load_wc_elo_ratings, get_elo_rating
      from alpha.data.ingestion.wc_stats import get_wc_team_stats

    logger = logging.getLogger(__name__)

    Module-level constants (per D-series decisions from CONTEXT.md and 06-PATTERNS.md):
      _WC_DRAW_RATE: float = 0.25
      _XG_ELO_SCALE: float = 35.0
      _XG_ADJ_CAP: float = 200.0
      KNOCKOUT_STAGES: frozenset[str] = frozenset({
          "LAST_16", "QUARTER_FINALS", "SEMI_FINALS", "THIRD_PLACE", "FINAL"
      })

    Class WCMatchModel:

      __init__(self, min_edge: float = 0.04):
        self.ev_calc = EVCalculator(min_edge=min_edge)
        self._elo_ratings: dict[str, int] = load_wc_elo_ratings()
          # Raises FileNotFoundError if wc_priors.json missing — no catch here per CONTEXT.md
        try:
            self._wc_stats: dict[str, dict] = get_wc_team_stats()
        except FileNotFoundError:
            logger.warning("WC stats cache not found — running Elo-only mode")
            self._wc_stats = {}

      predict(self, game: dict) -> dict:
        1. Guard: if game.get("league") != "wc": raise ValueError("WC model only accepts WC game dicts (league='wc')")
        2. Extract home_team = game.get("home_team", ""), away_team = game.get("away_team", "")
        3. Elo diff (neutral venue — NO +100 home-field boost per STATE.md v1.1 decision):
           elo_home = get_elo_rating(home_team, self._elo_ratings)
           elo_away = get_elo_rating(away_team, self._elo_ratings)
           elo_diff = elo_home - elo_away
        4. StatsBomb xG modifier (applied only when both teams present in self._wc_stats):
           if home_team in self._wc_stats and away_team in self._wc_stats:
               xg_diff = self._wc_stats[home_team]["avg_xG"] - self._wc_stats[away_team]["avg_xG"]
               elo_adj = elo_diff + xg_diff * _XG_ELO_SCALE
               elo_adj = max(-_XG_ADJ_CAP, min(_XG_ADJ_CAP, elo_adj))
           else:
               elo_adj = float(elo_diff)
        5. 2-way Elo-logistic probability:
           p_home_2way = 1.0 / (1.0 + 10.0 ** (-elo_adj / 400.0))
        6. Stage detection:
           stage = game.get("stage", "GROUP_STAGE")
           knockout = stage in KNOCKOUT_STAGES
        7. W/D/L decomposition:
           if knockout:
               p_draw = 0.0
               p_home = p_home_2way
               p_away = 1.0 - p_home_2way
           else:
               p_draw = _WC_DRAW_RATE
               p_home = p_home_2way * (1.0 - p_draw)
               p_away = (1.0 - p_home_2way) * (1.0 - p_draw)
        8. Market divergence flag (MODEL-04):
           home_odds = game.get("home_odds", -110)
           market_implied = self.ev_calc.implied_prob(
               self.ev_calc.american_to_decimal(home_odds)
           )
           elo_edge = abs(p_home - market_implied) > 0.05
        9. Append fields to game dict (do not create new dict — mutate input per CONTEXT.md):
           game["win_prob"] = round(p_home, 4)
           game["draw_prob"] = round(p_draw, 4)
           game["loss_prob"] = round(p_away, 4)
           game["elo_edge"] = elo_edge
           game["knockout"] = knockout
           game["model_name"] = "wc_elo_logistic"
           game["elo_diff"] = round(elo_adj, 2)
           return game

      evaluate_bet(self, game: dict) -> dict | None:
        game = self.predict(game)
        home_odds = game.get("home_odds", -110)
        home_dec = self.ev_calc.american_to_decimal(home_odds)
        if self.ev_calc.has_edge(game["win_prob"], home_dec):
            return game
        return None

      evaluate_batch(self, games: list[dict]) -> list[dict | None]:
        return [self.evaluate_bet(g) for g in games]

    Discretion choices (per CONTEXT.md "Claude's Discretion"):
    - Store home_elo and away_elo in output dict as "home_elo" and "away_elo" (useful for Phase 7 debugging)
    - Log info message on successful init: logger.info("WCMatchModel loaded %d Elo ratings, %d team stats", len(self._elo_ratings), len(self._wc_stats))
  </action>

  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/engines/test_wc_model.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - All tests in test_wc_model.py pass (0 failures, 0 errors)
    - Full suite does not regress: ./venv/Scripts/python.exe -m pytest --tb=no -q 2>&1 | tail -5 shows count >= 591 and "passed" with 0 failures
    - grep -c "soccer_model" alpha/engines/sports/wc_model.py returns 0
    - python -c "from alpha.engines.sports.wc_model import WCMatchModel, _WC_DRAW_RATE, KNOCKOUT_STAGES; print('ok')" exits 0 and prints "ok"
    - Test for soccer_model import guard passes: test_wc_model_does_not_import_soccer_model is in PASSED list
    - Test for knockout draw suppression passes: test_knockout_suppresses_draw is in PASSED list
    - Test for group-stage prob normalization passes: test_predict_probs_sum_to_one_group_stage is in PASSED list
  </acceptance_criteria>

  <done>
    wc_model.py committed. All 15+ test cases in test_wc_model.py pass. Full suite >= 606 tests
    passing with 0 regressions. GREEN phase confirmed. Commit message: "feat(06-01): implement WCMatchModel Elo-logistic match model"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game dict input → predict() | Caller-supplied dict; league field must be "wc" or ValueError raised |
| Elo ratings dict → __init__ | Read from data/wc_priors.json on disk; missing file raises FileNotFoundError (no silent fallback) |
| wc_stats dict → __init__ | Read from data/.wc_cache/wc_stats.pkl; missing file falls back to empty dict gracefully |
| home_odds / away_odds fields → EVCalculator | Missing fields default to -110 American; no user-controlled injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | predict() league guard | mitigate | ValueError raised immediately when game.get("league") != "wc" — non-WC game dicts cannot enter WC code path |
| T-06-02 | Tampering | wc_priors.json on disk | accept | File is read-only output of build_wc_priors.py one-time script; no user-controlled write path in Phase 6 scope |
| T-06-03 | Information Disclosure | elo_diff field in game dict | accept | Elo ratings are public data; exposing elo_diff aids Phase 7 debugging with no PII risk |
| T-06-04 | Denial of Service | large Elo ratings dict at init | accept | Dict is 48-entry JSON (WC teams); O(1) lookup; no DoS surface |
| T-06-05 | Elevation of Privilege | evaluate_bet() returns full game dict | accept | Dict contains no credentials or secrets; caller is wc_scanner.py (trusted internal code) |
| T-06-SC | Tampering | No new package installs in this plan | accept | No npm/pip installs; no slopcheck required |
</threat_model>

<verification>
Full phase checks after both tasks complete:

1. Test count gate:
   ./venv/Scripts/python.exe -m pytest --tb=no -q 2>&1 | tail -3
   Expected: count >= 606 passed, 0 failed

2. Soccer model isolation:
   grep -c "soccer_model" alpha/engines/sports/wc_model.py
   Expected: 0

3. WCMatchModel importable:
   ./venv/Scripts/python.exe -c "from alpha.engines.sports.wc_model import WCMatchModel; m = WCMatchModel.__new__(WCMatchModel); print('importable')"
   Expected: "importable" printed without error (does not init to avoid missing data file)

4. Key test cases present and passing:
   ./venv/Scripts/python.exe -m pytest tests/unit/engines/test_wc_model.py -v --tb=short 2>&1 | grep -E "(PASSED|FAILED)"
   Expected: 15+ PASSED entries, 0 FAILED

5. MODULE-level constants exported:
   ./venv/Scripts/python.exe -c "from alpha.engines.sports.wc_model import _WC_DRAW_RATE, KNOCKOUT_STAGES; assert _WC_DRAW_RATE == 0.25; assert 'FINAL' in KNOCKOUT_STAGES; print('constants ok')"
   Expected: "constants ok"

6. No regression in existing soccer/NBA/MLB test suites:
   ./venv/Scripts/python.exe -m pytest tests/unit/engines/ --tb=short -q 2>&1 | tail -5
   Expected: all existing tests still pass
</verification>

<success_criteria>
Phase 6 is complete when:
- wc_model.py exists at alpha/engines/sports/wc_model.py with WCMatchModel class
- test_wc_model.py exists at tests/unit/engines/test_wc_model.py with 15+ tests
- All 4 MODEL requirements satisfied:
  - MODEL-01: predict() outputs W/D/L via Elo-logistic with neutral venue (no +100 home boost)
  - MODEL-02: knockout stage sets draw_prob=0.0 and returns Win-to-Advance only
  - MODEL-03: stage field from game dict consumed; knockout bool added to output
  - MODEL-04: elo_edge=True when |win_prob - market_implied| > 0.05
- "soccer_model" does not appear anywhere in wc_model.py (grep returns 0)
- Full test suite >= 606 passing, 0 regressions
- Phase 7 can import WCMatchModel and call predict(game) on output from fetch_wc_games()
</success_criteria>

<output>
Create .planning/phases/06-match-model/06-01-SUMMARY.md when done.

Summary must include:
- What was built (class design, method signatures, constants)
- Key contracts delivered (predict() output fields, evaluate_bet() return type)
- Test count before and after
- Verification evidence (grep output for soccer_model, prob sum assertion result)
- Any discretion choices made (home_elo/away_elo in output dict, logging)
</output>
