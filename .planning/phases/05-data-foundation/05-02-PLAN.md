---
phase: 05-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alpha/data/ingestion/wc_elo.py
  - alpha/data/ingestion/wc_stats.py
  - tests/unit/test_wc_priors_loader.py
  - tests/unit/test_wc_stats.py
autonomous: true
requirements:
  - INGEST-02
  - INGEST-03

must_haves:
  truths:
    - "load_wc_elo_ratings() reads data/wc_priors.json and returns dict[str, int] keyed by team name"
    - "get_elo_rating(team, ratings) returns the team's rating or 1500 when the team is not in the dict"
    - "load_wc_elo_ratings() raises FileNotFoundError with 'build_wc_priors.py' in the message when wc_priors.json is missing"
    - "get_wc_team_stats() reads data/.wc_cache/wc_stats.pkl and returns dict[str, dict] with keys avg_goals, avg_xG, avg_shots, defense_score per team"
    - "get_wc_team_stats() raises FileNotFoundError with 'build_wc_priors.py' in the message when pkl is missing"
    - "_WC_CACHE_DIR path contains 'wc_cache' and does not contain 'soccer_cache' — namespaces are isolated"
    - "built_at key is popped from the returned dict so callers never see it"
  artifacts:
    - path: "alpha/data/ingestion/wc_elo.py"
      provides: "Elo ratings JSON loader"
      exports:
        - "load_wc_elo_ratings"
        - "get_elo_rating"
        - "_WC_PRIORS_PATH"
        - "_ELO_FALLBACK"
    - path: "alpha/data/ingestion/wc_stats.py"
      provides: "StatsBomb WC team stats pkl loader"
      exports:
        - "get_wc_team_stats"
        - "_WC_CACHE_DIR"
        - "_WC_STATS_CACHE"
    - path: "tests/unit/test_wc_priors_loader.py"
      provides: "Unit tests for wc_elo.py"
      contains: "test_load_wc_priors_returns_dict"
    - path: "tests/unit/test_wc_stats.py"
      provides: "Unit tests for wc_stats.py"
      contains: "test_get_wc_team_stats_loads_pkl"
  key_links:
    - from: "alpha/data/ingestion/wc_elo.py"
      to: "data/wc_priors.json"
      via: "json.load()"
      pattern: "_WC_PRIORS_PATH"
    - from: "alpha/data/ingestion/wc_stats.py"
      to: "data/.wc_cache/wc_stats.pkl"
      via: "pickle.load()"
      pattern: "_WC_STATS_CACHE"
---

<objective>
Create two new reader modules — `wc_elo.py` and `wc_stats.py` — that give downstream code (Phase 6 wc_model.py) clean access to the pre-built Elo ratings and StatsBomb team stats without making any network calls.

Purpose: The live scanner must never call StatsBomb or eloratings.net at runtime. These modules are pure file-readers that load from `data/wc_priors.json` and `data/.wc_cache/wc_stats.pkl` respectively. Phase 3's build_wc_priors.py populates those files; this plan ensures the reader interfaces are correct and tested before Phase 6 consumes them.

Output: `alpha/data/ingestion/wc_elo.py`, `alpha/data/ingestion/wc_stats.py`, and unit test files for both (12+ tests total). No network calls in any of these files.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Users/justi/Documents/money-maker/.planning/PROJECT.md
@c:/Users/justi/Documents/money-maker/.planning/ROADMAP.md
@c:/Users/justi/Documents/money-maker/.planning/STATE.md

<interfaces>
<!-- Key patterns extracted from analogs — executor must read the source files for full context -->

From alpha/data/ingestion/soccer_stats.py (cache pattern analog):
```python
_CACHE_DIR = Path("data/.soccer_cache")   # ← WC uses Path("data/.wc_cache")

def _cache_path(key: str) -> Path:
    _CACHE_DIR.mkdir(parents=True, exist_ok=True)
    return _CACHE_DIR / f"{key}_{date.today()}.pkl"   # ← WC: fixed "wc_stats.pkl", no date suffix

def _load_cache(key: str) -> Any | None:
    path = _cache_path(key)
    if path.exists():
        with open(path, "rb") as f:
            return pickle.load(f)
    return None
```

Intended interface for wc_elo.py:
```python
_WC_PRIORS_PATH = Path("data/wc_priors.json")
_ELO_FALLBACK = 1500

def load_wc_elo_ratings() -> dict[str, int]:
    """Raises FileNotFoundError if file missing. Returns {team_name: elo_rating}."""

def get_elo_rating(team: str, ratings: dict[str, int]) -> int:
    """Returns rating from dict or _ELO_FALLBACK if team not present."""
```

Intended interface for wc_stats.py:
```python
_WC_CACHE_DIR = Path("data/.wc_cache")
_WC_STATS_CACHE = _WC_CACHE_DIR / "wc_stats.pkl"

_TEAM_NAME_MAP: dict[str, str] = {}   # StatsBomb name → football-data.org name

def get_wc_team_stats() -> dict[str, dict]:
    """
    Returns {team_name: {"avg_goals": float, "avg_xG": float,
                          "avg_shots": float, "defense_score": float}}
    Raises FileNotFoundError if pkl missing.
    Pops "built_at" key before returning.
    Applies _TEAM_NAME_MAP normalization.
    """
```

Test pattern from tests/unit/test_mlb_stats.py (tmp_path + pkl fixture):
```python
def test_something(tmp_path, monkeypatch):
    monkeypatch.setattr("alpha.data.ingestion.wc_stats._WC_STATS_CACHE",
                        tmp_path / "wc_stats.pkl")
    monkeypatch.setattr("alpha.data.ingestion.wc_stats._WC_CACHE_DIR", tmp_path)
    # write fixture pkl, call function, assert
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create wc_elo.py (Elo JSON reader) and its unit tests</name>
  <files>alpha/data/ingestion/wc_elo.py, tests/unit/test_wc_priors_loader.py</files>
  <read_first>
    - alpha/data/ingestion/soccer_stats.py — read lines 1-69 to understand imports block, logging pattern, cache constants pattern, and _load_cache structure to mirror in wc_elo.py
    - tests/unit/test_mlb_stats.py — read lines 1-60 to understand tmp_path + monkeypatch pattern for file-reader tests
    - .planning/phases/05-data-foundation/05-PATTERNS.md — sections "wc_elo.py (utility, file-I/O)" and "tests/unit/test_wc_priors_loader.py" — concrete code for both files
  </read_first>
  <behavior>
    - load_wc_elo_ratings(): if _WC_PRIORS_PATH does not exist, raises FileNotFoundError with message containing "build_wc_priors.py"; on success returns dict[str, int] from json.load()
    - get_elo_rating(team, ratings): returns ratings[team] if present; returns _ELO_FALLBACK (1500) if not; logs a warning on fallback
    - test_load_wc_priors_returns_dict: writes {"Brazil": 2100, "Germany": 1980} to tmp_path/wc_priors.json, monkeypatches _WC_PRIORS_PATH; asserts result["Brazil"] == 2100
    - test_elo_fallback_1500: get_elo_rating("Zimbabwe", {"Brazil": 2100}) == 1500; get_elo_rating("Brazil", {"Brazil": 2100}) == 2100
    - test_missing_priors_raises: monkeypatches _WC_PRIORS_PATH to nonexistent file; pytest.raises(FileNotFoundError, match="build_wc_priors.py")
    - test_load_wc_priors_elo_fallback_constant: _ELO_FALLBACK == 1500
    - test_load_wc_priors_path_is_data_dir: str(_WC_PRIORS_PATH) contains "wc_priors.json"
  </behavior>
  <action>
    Create alpha/data/ingestion/wc_elo.py. Module docstring: "Elo ratings reader for WC 2026 national teams. Reads from data/wc_priors.json written by scripts/build_wc_priors.py."

    Imports: `from __future__ import annotations`, `import json`, `import logging`, `from pathlib import Path`. Logger: `logger = logging.getLogger(__name__)`.

    Constants: `_WC_PRIORS_PATH = Path("data/wc_priors.json")` and `_ELO_FALLBACK: int = 1500`.

    Function `load_wc_elo_ratings() -> dict[str, int]`: check `_WC_PRIORS_PATH.exists()`, raise `FileNotFoundError(f"WC Elo priors not found at {_WC_PRIORS_PATH}. Run: ./venv/Scripts/python.exe scripts/build_wc_priors.py")` if missing. Open with `encoding="utf-8"`, json.load, log debug with team count, return the dict.

    Function `get_elo_rating(team: str, ratings: dict[str, int]) -> int`: return `ratings.get(team, _ELO_FALLBACK)`. If falling back, log `logger.warning("No Elo rating for '%s' — using fallback %d", team, _ELO_FALLBACK)`. Do not make any network calls.

    Then create tests/unit/test_wc_priors_loader.py. Import block: `from __future__ import annotations`, `import json`, `from pathlib import Path`, `import pytest`, `from alpha.data.ingestion.wc_elo import load_wc_elo_ratings, get_elo_rating, _ELO_FALLBACK`. Write tests per the behavior block above. For monkeypatch tests, use `monkeypatch.setattr("alpha.data.ingestion.wc_elo._WC_PRIORS_PATH", tmp_path / "wc_priors.json")`.
  </action>
  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/test_wc_priors_loader.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - alpha/data/ingestion/wc_elo.py exists with load_wc_elo_ratings() and get_elo_rating()
    - tests/unit/test_wc_priors_loader.py exists with 5+ tests all passing
    - No network calls anywhere in wc_elo.py (grep -n "requests\|urllib\|http" alpha/data/ingestion/wc_elo.py returns empty)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create wc_stats.py (StatsBomb pkl reader) and its unit tests</name>
  <files>alpha/data/ingestion/wc_stats.py, tests/unit/test_wc_stats.py</files>
  <read_first>
    - alpha/data/ingestion/soccer_stats.py — full file read; wc_stats.py is a simplified version of this pattern (same pickle load idiom, same logger, but no async, no date-keyed cache filenames, no network calls)
    - .planning/phases/05-data-foundation/05-PATTERNS.md — sections "wc_stats.py (service, file-I/O)" and "tests/unit/test_wc_stats.py" — concrete code for both files including the _TEAM_NAME_MAP normalization step and built_at pop
    - tests/unit/test_mlb_stats.py — tmp_path + pkl monkeypatch pattern; lines 1-70 sufficient
  </read_first>
  <behavior>
    - _WC_CACHE_DIR = Path("data/.wc_cache") — contains "wc_cache" in path, does NOT contain "soccer_cache"
    - _WC_STATS_CACHE = _WC_CACHE_DIR / "wc_stats.pkl"
    - _TEAM_NAME_MAP: dict[str, str] = {} — empty initially; populated after Phase 5 build run reveals team name mismatches
    - get_wc_team_stats(): if _WC_STATS_CACHE does not exist, raises FileNotFoundError with "build_wc_priors.py" in message; on success, pickle.load, pops "built_at" key (logs debug with built_at value and team count), applies _TEAM_NAME_MAP normalization (mapped_name = _TEAM_NAME_MAP.get(team, team)), returns normalized dict
    - Output dict per team: exactly {"avg_goals": float, "avg_xG": float, "avg_shots": float, "defense_score": float}
    - test_get_wc_team_stats_loads_pkl: fake pkl with {"Brazil": {4 keys}, "built_at": "..."}, monkeypatched path; assert "Brazil" in result and result["Brazil"]["avg_goals"] == approx(2.1) and "built_at" not in result
    - test_get_wc_team_stats_missing_pkl_raises: monkeypatched to nonexistent path; pytest.raises(FileNotFoundError, match="build_wc_priors.py")
    - test_wc_stats_output_shape: result["Germany"] keys == {"avg_goals", "avg_xG", "avg_shots", "defense_score"}
    - test_wc_cache_path_isolated: "wc_cache" in str(_WC_CACHE_DIR) and "soccer_cache" not in str(_WC_CACHE_DIR) and "wc_stats.pkl" in str(_WC_STATS_CACHE)
    - test_wc_stats_team_name_map_applied: fake pkl with {"United States Men's National Team": {...}}, _TEAM_NAME_MAP monkeypatched to {"United States Men's National Team": "United States"}; assert "United States" in result
    - test_defense_score_per_game: result["Germany"]["defense_score"] == approx(0.9) (per-game, not total)
  </behavior>
  <action>
    Create alpha/data/ingestion/wc_stats.py. Module docstring: "WC team stats reader — loads pre-built StatsBomb aggregated data from data/.wc_cache/wc_stats.pkl. Cache is written once by scripts/build_wc_priors.py; this module only reads."

    Imports: `from __future__ import annotations`, `import logging`, `import pickle`, `from pathlib import Path`. Logger: `logger = logging.getLogger(__name__)`.

    Constants: `_WC_CACHE_DIR = Path("data/.wc_cache")`, `_WC_STATS_CACHE = _WC_CACHE_DIR / "wc_stats.pkl"`.

    `_TEAM_NAME_MAP: dict[str, str] = {}` — with a comment: "Maps StatsBomb team names to football-data.org team names. Populate after first build_wc_priors.py run by comparing sb.matches() team names to fixture response."

    Function `get_wc_team_stats() -> dict[str, dict]`: check `_WC_STATS_CACHE.exists()`, raise FileNotFoundError with "WC stats cache not found at {_WC_STATS_CACHE}. Run: ./venv/Scripts/python.exe scripts/build_wc_priors.py" if missing. Open in "rb", pickle.load into `data: dict`. Pop `data.pop("built_at", "unknown")` into `built_at`, log debug "WC stats cache loaded (built_at=%s, teams=%d)". Apply normalization: iterate `data.items()`, for each (team, stats) compute `canonical = _TEAM_NAME_MAP.get(team, team)`, assign to output dict. Return normalized dict. No network calls anywhere.

    Create tests/unit/test_wc_stats.py. Import block: `from __future__ import annotations`, `import pickle`, `from pathlib import Path`, `import pytest`, `from alpha.data.ingestion.wc_stats import get_wc_team_stats, _WC_STATS_CACHE, _WC_CACHE_DIR`. For monkeypatch tests, patch both `_WC_STATS_CACHE` and `_WC_CACHE_DIR` to use tmp_path. The team_name_map test requires also monkeypatching `alpha.data.ingestion.wc_stats._TEAM_NAME_MAP`. Write all 6 tests per behavior block.
  </action>
  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/test_wc_stats.py tests/unit/test_wc_priors_loader.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - alpha/data/ingestion/wc_stats.py exists with get_wc_team_stats(), _WC_CACHE_DIR, _WC_STATS_CACHE, _TEAM_NAME_MAP
    - tests/unit/test_wc_stats.py exists with 6+ tests all passing
    - No network calls in wc_stats.py (grep confirms)
    - Full test suite passes: ./venv/Scripts/python.exe -m pytest tests/ -x -q shows 578+ passing, 0 failed (566 baseline + tests from plan 01 + tests from this plan)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → wc_elo.py | Reads data/wc_priors.json — written by build_wc_priors.py, not user input |
| filesystem → wc_stats.py | Reads data/.wc_cache/wc_stats.pkl — written by build_wc_priors.py, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | wc_stats.pkl deserialization (pickle.load) | accept | File is written by our own build_wc_priors.py script in the same repo; not user-supplied; pickle.load of own data is standard Python pattern. If file integrity is critical in future, add SHA-256 check. |
| T-05-05 | Information Disclosure | wc_priors.json | accept | Elo ratings are public data; no PII; JSON is human-readable — this is intentional for debuggability |
| T-05-06 | Denial of Service | FileNotFoundError on missing cache | mitigate | Both modules raise FileNotFoundError with clear action message rather than hanging or returning silent zero-defaults; Phase 6 callers handle this gracefully |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | statsbombpy install happens in Plan 03, not this plan; no installs in this plan |
</threat_model>

<verification>
1. `./venv/Scripts/python.exe -m pytest tests/unit/test_wc_stats.py tests/unit/test_wc_priors_loader.py -v` — all 11+ tests pass
2. `./venv/Scripts/python.exe -m pytest tests/ -x -q` — full suite green, zero regressions
3. Grep: `grep -n "requests\|urllib\|http" alpha/data/ingestion/wc_elo.py alpha/data/ingestion/wc_stats.py` — empty (no network calls in reader modules)
4. Grep: `grep -n "_WC_CACHE_DIR\|_WC_STATS_CACHE" alpha/data/ingestion/wc_stats.py` — confirms path constants do not reference ".soccer_cache"
5. Grep: `grep -n "def load_wc_elo_ratings\|def get_elo_rating" alpha/data/ingestion/wc_elo.py` — both functions present
6. Grep: `grep -n "def get_wc_team_stats" alpha/data/ingestion/wc_stats.py` — function present
</verification>

<success_criteria>
- wc_elo.py: load_wc_elo_ratings() raises FileNotFoundError on missing file; returns dict[str, int] from JSON on success; get_elo_rating() returns 1500 for unknown teams
- wc_stats.py: get_wc_team_stats() raises FileNotFoundError on missing pkl; returns dict per team with exactly {avg_goals, avg_xG, avg_shots, defense_score}; pops built_at; applies _TEAM_NAME_MAP; cache path is data/.wc_cache/, not data/.soccer_cache/
- 11+ unit tests across both test files, all passing
- Full suite at 578+ passing, zero regressions
</success_criteria>

<output>
Create `.planning/phases/05-data-foundation/05-02-SUMMARY.md` when done
</output>
