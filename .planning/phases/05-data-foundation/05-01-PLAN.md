---
phase: 05-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alpha/data/ingestion/football_data_client.py
  - tests/unit/data/test_football_data_client_wc.py
autonomous: true
requirements:
  - INGEST-01

must_haves:
  truths:
    - "fetch_wc_games(date_from, date_to) returns a list of dicts each containing home_team, away_team, league='wc', stage, and group fields"
    - "A 429 response on the first attempt triggers a 60s wait and exactly one retry before returning []"
    - "_COMP_MAP contains 'wc': 'WC' without removing or altering 'epl' or 'ucl' entries"
    - "fetch_today_games() for EPL and UCL behaves exactly as before — no regression"
    - "When api_key is empty, fetch_wc_games() returns [] immediately with a warning log"
  artifacts:
    - path: "alpha/data/ingestion/football_data_client.py"
      provides: "FootballDataClient with fetch_wc_games() and module-level _get_with_retry()"
      exports:
        - "_COMP_MAP"
        - "_get_with_retry"
        - "FootballDataClient.fetch_wc_games"
    - path: "tests/unit/data/test_football_data_client_wc.py"
      provides: "Unit tests for WC fixture ingestion and 429 retry"
      contains: "test_fetch_wc_games_returns_stage_and_group"
  key_links:
    - from: "alpha/data/ingestion/football_data_client.py"
      to: "https://api.football-data.org/v4/competitions/WC/matches"
      via: "_get_with_retry()"
      pattern: "competitions/WC/matches"
---

<objective>
Add WC 2026 fixture ingestion to FootballDataClient: one-line `_COMP_MAP` update, a module-level 429 retry helper, and a new `fetch_wc_games(date_from, date_to)` method that returns game dicts with `stage` and `group` fields.

Purpose: Phase 6 (wc_model.py) and Phase 7 (wc_scanner.py) need live WC fixtures with stage metadata to route group-stage vs. knockout logic. This plan is the data entry point for the entire WC pipeline.

Output: Modified `football_data_client.py` with WC support; new test file with 10+ unit tests covering the happy path, 429 retry, error paths, and _COMP_MAP regression.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Users/justi/Documents/money-maker/.planning/PROJECT.md
@c:/Users/justi/Documents/money-maker/.planning/ROADMAP.md
@c:/Users/justi/Documents/money-maker/.planning/STATE.md

<interfaces>
<!-- Key contracts extracted from football_data_client.py (current state) -->
<!-- Executor must read the file before modifying — use as source of truth for method signatures and error-handling patterns -->

From alpha/data/ingestion/football_data_client.py:

Module-level constants (current):
```
_BASE_URL = "https://api.football-data.org/v4"

_COMP_MAP: dict[str, str] = {
    "epl": "PL",   # English Premier League
    "ucl": "CL",   # UEFA Champions League
}
```

Class interface:
```python
class FootballDataClient:
    def __init__(self, api_key: str | None = None): ...
    def is_configured(self) -> bool: ...
    def fetch_today_games(self, league_key: str) -> list[dict]: ...
```

fetch_today_games() game dict shape (established contract — do not alter):
```python
{
    "home_team": str,
    "away_team": str,
    "home_odds": int,    # -110 default
    "away_odds": int,    # -110 default
    "league": str,
    "event_id": str,
    "commence_time": str,
}
```

Error handling pattern (lines 100-105 — replicate exactly for WC):
```python
except requests.exceptions.HTTPError as exc:
    status = exc.response.status_code if exc.response is not None else "?"
    logger.warning("football-data.org HTTP %s for %s", status, league_key)
except Exception as exc:
    logger.warning("football-data.org error for %s: %s", league_key, exc)
return []
```

Test imports pattern (from tests/unit/data/test_odds_api.py — use same structure):
```python
from unittest.mock import MagicMock, patch
import pytest
import requests as req_lib
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _get_with_retry helper and fetch_wc_games() to FootballDataClient</name>
  <files>alpha/data/ingestion/football_data_client.py</files>
  <read_first>
    - alpha/data/ingestion/football_data_client.py — read in full before touching; this is the source of truth for _BASE_URL, _COMP_MAP structure, FootballDataClient class, and error-handling patterns that fetch_wc_games() must replicate
    - .planning/phases/05-data-foundation/05-PATTERNS.md — sections "football_data_client.py (service)" and "Shared Patterns: HTTP Request Pattern" — concrete code excerpts for all four changes
  </read_first>
  <behavior>
    - _COMP_MAP["wc"] == "WC" after the change
    - _COMP_MAP["epl"] == "PL" and _COMP_MAP["ucl"] == "CL" are unchanged
    - _get_with_retry(url, *, headers, params, timeout=10): on status_code==429 and attempt==0, calls time.sleep(60) and retries once; on any other non-200 status, calls raise_for_status() immediately; returns a Response on success
    - fetch_wc_games("2026-06-12", "2026-06-14") hits GET https://api.football-data.org/v4/competitions/WC/matches with params dateFrom and dateTo
    - Returned game dict shape: {"home_team": str, "away_team": str, "home_odds": -110, "away_odds": -110, "league": "wc", "event_id": str, "commence_time": str, "stage": str, "group": str}
    - matches missing homeTeam.name or awayTeam.name are skipped silently
    - stage = match.get("stage", ""), group = match.get("group", "") — both default to ""
    - When is_configured() is False, fetch_wc_games() returns [] with a logger.warning, never calls _get_with_retry
    - On HTTPError (non-429): logs warning with status code, returns []
    - On any other Exception: logs warning with exc message, returns []
    - `import time` added to module-level imports
  </behavior>
  <action>
    Read football_data_client.py first to understand exact line positions. Make four targeted changes:

    1. Add `import time` to the imports block (after `import requests`, before `logger = ...`).

    2. Add `"wc": "WC",  # FIFA World Cup` entry to _COMP_MAP after the "ucl" line (add a trailing comment for alignment).

    3. After the _COMP_MAP block and before the `class FootballDataClient:` definition, insert the module-level `_get_with_retry(url, *, headers, params, timeout=10)` function. The function loops `for attempt in range(2)`: calls `requests.get(url, headers=headers, params=params, timeout=timeout)`, checks `resp.status_code == 429 and attempt == 0` to sleep 60s and continue, otherwise calls `resp.raise_for_status()` and returns `resp`. After the loop falls through (second attempt also 429), calls `resp.raise_for_status()` and returns `resp`.

    4. Add `fetch_wc_games(self, date_from: str, date_to: str) -> list[dict]` method to the FootballDataClient class. It guards on `is_configured()` (return [] + warning if False), then wraps `_get_with_retry(f"{_BASE_URL}/competitions/WC/matches", headers={"X-Auth-Token": self.api_key}, params={"dateFrom": date_from, "dateTo": date_to}, timeout=10)` in try/except. On success, iterates `data.get("matches", [])`, extracts home/away names, skips if either is empty, and appends a dict with the 9-field WC game shape (home_team, away_team, home_odds=-110, away_odds=-110, league="wc", event_id=str(match.get("id","")), commence_time=match.get("utcDate",""), stage=match.get("stage",""), group=match.get("group","")). Logs `"Fetched %d WC games from football-data.org"` and returns the list. Exception handlers mirror fetch_today_games() error-handling pattern exactly.

    Never log self.api_key. Never modify fetch_today_games() or any existing method.
  </action>
  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/data/test_football_data_client_wc.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _COMP_MAP has "wc": "WC" alongside unchanged "epl" and "ucl" entries
    - _get_with_retry is a module-level function in football_data_client.py
    - fetch_wc_games(date_from, date_to) method exists on FootballDataClient
    - All tests in test_football_data_client_wc.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for fetch_wc_games() and 429 retry</name>
  <files>tests/unit/data/test_football_data_client_wc.py</files>
  <read_first>
    - tests/unit/data/test_odds_api.py — HTTP mock pattern using MagicMock + patch("alpha.data.ingestion.football_data_client.requests.get"), _make_event() helper pattern to replicate as _make_match_response()
    - .planning/phases/05-data-foundation/05-PATTERNS.md — section "tests/unit/data/test_football_data_client_wc.py" — exact test bodies for all 8+ test cases, import block, fixture helper
    - alpha/data/ingestion/football_data_client.py — (after Task 1 completes) to confirm _COMP_MAP and fetch_wc_games() are importable under the exact names used in the tests
  </read_first>
  <behavior>
    - test_comp_map_has_wc: assert "wc" in _COMP_MAP and _COMP_MAP["wc"] == "WC"
    - test_comp_map_epl_ucl_unchanged: assert _COMP_MAP["epl"] == "PL" and _COMP_MAP["ucl"] == "CL"
    - test_fetch_wc_games_returns_stage_and_group: mock returns one match with stage="GROUP_STAGE", group="Group A"; assert games[0]["stage"] == "GROUP_STAGE" and games[0]["group"] == "Group A" and games[0]["league"] == "wc"
    - test_fetch_wc_games_knockout_no_group: mock returns match with stage="LAST_16", group=""; assert games[0]["stage"] == "LAST_16" and games[0]["group"] == ""
    - test_fetch_wc_games_skips_missing_teams: mock returns two matches, one with empty homeTeam.name; assert len(games) == 1
    - test_fetch_wc_games_429_retry: first requests.get returns resp_429 (status_code=429), second returns resp_200 with one match; monkeypatch time.sleep; assert len(games) == 1 and sleep was called with 60
    - test_fetch_wc_games_returns_empty_on_http_error: requests.get raises HTTPError (status 500); assert result == []
    - test_fetch_wc_games_returns_empty_when_not_configured: FootballDataClient(api_key=""); assert result == []
    - test_fetch_wc_games_event_id_is_string: assert isinstance(games[0]["event_id"], str)
    - test_fetch_today_games_epl_regression: mock returns one EPL match; assert game dict does NOT have "stage" or "group" keys — fetch_today_games() is unchanged
  </behavior>
  <action>
    Create tests/unit/data/test_football_data_client_wc.py from scratch. Import block: `from unittest.mock import MagicMock, patch`, `import pytest`, `import requests as req_lib`, `from alpha.data.ingestion.football_data_client import FootballDataClient, _COMP_MAP`.

    Define `_make_match_response(home, away, match_id=1, stage="GROUP_STAGE", group="Group A")` helper that returns `{"matches": [{"id": match_id, "utcDate": "2026-06-12T18:00:00Z", "stage": stage, "group": group, "homeTeam": {"name": home}, "awayTeam": {"name": away}}]}`.

    For HTTP mock tests, patch path is `"alpha.data.ingestion.football_data_client.requests.get"`. For the 429 retry test, monkeypatch `"alpha.data.ingestion.football_data_client.time.sleep"` to capture calls.

    The 429 retry test passes `side_effect=[resp_429, resp_200]` to the patch — resp_429 has status_code=429 and raise_for_status that does NOT raise (the retry logic checks status_code directly), resp_200 has status_code=200, raise_for_status=no-op, json returns valid match data.

    Regression test for fetch_today_games(): mock returns a minimal EPL match response (no stage/group fields in response body); assert the returned game dict keys are exactly {"home_team", "away_team", "home_odds", "away_odds", "league", "event_id", "commence_time"} with no "stage" or "group" keys.

    Write tests BEFORE running them (TDD: red first, then Task 1 makes them green).
  </action>
  <verify>
    <automated>./venv/Scripts/python.exe -m pytest tests/unit/data/test_football_data_client_wc.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_football_data_client_wc.py exists with 10+ test functions
    - All 10+ tests pass
    - Full suite still passes: ./venv/Scripts/python.exe -m pytest tests/ -x -q shows 576+ passing (566 baseline + new tests), 0 failed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alpha code → football-data.org API | Untrusted HTTP response body; parsed via .json() which can raise on malformed JSON |
| environment → FootballDataClient | FOOTBALL_API_KEY read from env; never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | football_data_client.py logger | mitigate | Never log self.api_key; existing pattern already avoids this; confirm no new log statements include the key |
| T-05-02 | Tampering | football-data.org response body | accept | Official API, HTTPS; match fields (stage, group) parsed via safe dict.get() with empty-string defaults; no eval or exec |
| T-05-03 | Denial of Service | 429 rate limit | mitigate | 1-retry + 60s backoff in _get_with_retry(); logs warning; returns [] on persistent 429 so scanner degrades gracefully |
| T-05-SC | Tampering | statsbombpy install (Phase 03) | mitigate | Package legitimacy audit in RESEARCH.md returned [OK] for statsbombpy 1.19.0 — no checkpoint needed for this plan |
</threat_model>

<verification>
1. `./venv/Scripts/python.exe -m pytest tests/unit/data/test_football_data_client_wc.py -v` — all WC client tests pass
2. `./venv/Scripts/python.exe -m pytest tests/ -x -q` — full suite green (566 baseline + new tests), zero regressions
3. Grep check: `grep -n "wc.*WC\|WC.*wc" alpha/data/ingestion/football_data_client.py` — confirms "wc": "WC" in _COMP_MAP
4. Grep check: `grep -n "def fetch_wc_games\|def _get_with_retry" alpha/data/ingestion/football_data_client.py` — confirms both definitions exist
5. Grep check: `grep -n "api_key" alpha/data/ingestion/football_data_client.py | grep -v "self.api_key\|os.environ\|#"` — should show no bare api_key in log statements
</verification>

<success_criteria>
- FootballDataClient.fetch_wc_games(date_from, date_to) returns list[dict] with "stage" and "group" keys per game
- _get_with_retry is a module-level helper that retries once after 60s on 429
- _COMP_MAP["wc"] == "WC" without touching existing entries
- 10+ unit tests in test_football_data_client_wc.py all passing
- Full test suite at 576+ passing, zero regressions
</success_criteria>

<output>
Create `.planning/phases/05-data-foundation/05-01-SUMMARY.md` when done
</output>
