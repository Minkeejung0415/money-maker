# Phase 43 Plan

## Goal

Add the route-offset contract and baseline harness without changing default production picks.

## Tasks

1. Add a route-offset engine with explicit schema/config identity.
2. Return fallback diagnostics for missing, invalid, stale, and schema-mismatched snapshots.
3. Preserve baseline scoreline behavior unless route offsets are explicitly applied.
4. Add tests for fallback and identity mismatch.

## Verification

- Unit tests for fail-closed route-offset contracts.
- Scanner output keeps route-offset in shadow mode by default.
